---
// Video Rekomendasi — 러시아어판 ru/videos.astro와 같은 구조 (2026-09-06 신설).
//
// 영상 자체는 한국어판·일본어판·러시아어판과 같은 12편(연구소장이 직접 만든
// 입문 시리즈)이고, 제목만 인도네시아어로 옮겼다. 음성이 한국어라는 사실은
// 재생 전에 알린다 — 보고 나서 알게 하면 신뢰를 해친다.
import TwLayout from '../../layouts/TwLayout.astro';
import { videosTw, videosMetaTw, videosLocalTw } from '../../data/tw/pages';
import twI18n from '../../i18n/tw.json';
import { YOUTUBE_CHANNEL_URL } from '../../utils/site';

const p = videosMetaTw;

// ⚠️ VideoObject는 쓰지 않는다. uploadDate·duration을 정확히 모르기 때문이다
//    (recommended-videos.json의 addedAt은 사이트 추가일이지 유튜브 공개일이
//    아니다). 화면에 보이는 목록 순서를 ItemList로만 전달한다 — ru판과 동일.
const schema = [
  {
    '@type': 'ItemList',
    name: p.pageTitle,
    numberOfItems: videosTw.length,
    itemListElement: videosTw.map((v, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: v.titleTw,
      url: `https://www.youtube.com/watch?v=${v.videoId}`,
    })),
  },
  {
    '@type': 'ItemList',
    name: '其他頻道的推薦影片',
    numberOfItems: videosLocalTw.length,
    itemListElement: videosLocalTw.map((v, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: v.title,
      url: `https://www.youtube.com/watch?v=${v.videoId}`,
    })),
  },
];
---

<TwLayout
  title={p.title}
  description={p.description}
  crumb={twI18n.nav.videos}
  koPath="/youtube/"
  idPath="/id/videos/"
  jaPath="/ja/videos/"
  ruPath="/ru/videos/"
  schema={schema}
>
  <h1>{p.pageTitle}</h1>
  <p class="tw-lead">{p.lead}</p>

  {/* 언어 안내는 재생 전에 */}
  <p class="tw-notice">{p.notice}</p>

  <p class="tw-count">Jumlah video: {videosTw.length}</p>

  <h2 class="tw-vsec">本站作者自製的影片</h2>
  <ul class="tw-videos">
    {
      videosTw.map((v) => (
        <li>
          <a href={`https://www.youtube.com/watch?v=${v.videoId}`} target="_blank" rel="noopener">
            <img
              src={`https://img.youtube.com/vi/${v.videoId}/hqdefault.jpg`}
              alt=""
              loading="lazy"
              width="320"
              height="180"
            />
            <div class="tw-video-text">
              <span class="tw-video-title">{v.titleTw}</span>
              {v.note && <span class="tw-video-note">{v.note}</span>}
              <span class="tw-video-ext">
                Tonton di YouTube<span class="sr-only"> ({twI18n.ui.external})</span>
              </span>
            </div>
          </a>
        </li>
      ))
    }
  </ul>


  <h2 class="tw-vsec">其他頻道的推薦影片</h2>
  <p class="tw-lead">以繁體中文談醫療 AI 與智慧醫療的影片，這些不是本站作者製作的。</p>
  {/* 제휴가 아니라는 안내를 반드시 함께 낸다 — 지우지 말 것.
      영상 내용은 각 제작자의 것이고 이 사이트가 검증한 주장이 아니다. */}
  <p class="tw-notice">以下影片都屬於各頻道的創作者。列在這裡只代表本站選了它，與對方沒有合作或贊助關係。影片中的說法是創作者的意見，本站沒有查證。</p>

  <ul class="tw-videos">
    {
      videosLocalTw.map((v) => (
        <li>
          <a href={`https://www.youtube.com/watch?v=${v.videoId}`} target="_blank" rel="noopener">
            <img
              src={`https://img.youtube.com/vi/${v.videoId}/hqdefault.jpg`}
              alt=""
              loading="lazy"
              width="320"
              height="180"
            />
            <div class="tw-video-text">
              <span class="tw-video-title">{v.title}</span>
              <span class="tw-video-ch">{v.channel}</span>
              {v.note && <span class="tw-video-note">{v.note}</span>}
              <span class="tw-video-ext">
                在 YouTube 觀看<span class="sr-only"> ({twI18n.ui.external})</span>
              </span>
            </div>
          </a>
        </li>
      ))
    }
  </ul>

  <p class="tw-channel">
    <a href={YOUTUBE_CHANNEL_URL} target="_blank" rel="noopener">
      {twI18n.profile.youtube}<span class="sr-only"> ({twI18n.ui.external})</span>
    </a>
  </p>
</TwLayout>

<style>
  .tw-lead {
    color: var(--tw-text-sub);
  }
  .tw-notice {
    border: 1px solid var(--tw-border);
    border-left: 4px solid var(--tw-primary);
    background: var(--tw-surface);
    padding: 12px 14px;
    font-size: 13.5px;
    line-height: 1.85;
    margin-bottom: 18px;
  }
  .tw-count {
    font-size: 13px;
    color: var(--tw-text-sub);
    margin-bottom: 16px;
  }
  .tw-videos {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .tw-videos a {
    display: flex;
    gap: 14px;
    align-items: flex-start;
    border: 1px solid var(--tw-border);
    background: var(--tw-base);
    padding: 12px;
    text-decoration: none;
    color: var(--tw-text);
  }
  .tw-videos a:hover {
    background: var(--tw-surface);
    border-color: var(--tw-primary);
  }
  .tw-videos img {
    width: 160px;
    height: 90px;
    object-fit: cover;
    flex-shrink: 0;
    background: var(--tw-surface);
  }
  .tw-video-text {
    display: flex;
    flex-direction: column;
    gap: 5px;
    min-width: 0;
  }
  .tw-video-title {
    font-size: 14.5px;
    line-height: 1.7;
    color: var(--tw-primary);
  }
  .tw-video-note {
    font-size: 13px;
    color: var(--tw-text-sub);
    line-height: 1.8;
  }
  .tw-video-ext {
    font-size: 12px;
    color: var(--tw-text-sub);
  }
  .tw-channel {
    margin-top: 24px;
    padding-top: 16px;
    border-top: 1px solid var(--tw-border);
    font-size: 14px;
  }

  @media (max-width: 560px) {
    .tw-videos a {
      flex-direction: column;
    }
    .tw-videos img {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 9;
    }
  }
  .tw-vsec {
    font-size: 16px;
    margin: 30px 0 10px;
    padding-left: 10px;
    border-left: 4px solid var(--tw-primary);
  }
  .tw-vsec:first-of-type {
    margin-top: 18px;
  }
  .tw-video-ch {
    font-size: 12.5px;
    color: var(--tw-text-sub);
  }
</style>

---
// 인도네시아어판 홈 (Fase A — 2026-09-05 신설).
//
// 2026-09-06: 뉴스·정부 프로그램·추천 영상이 생기면서 카드 그리드를 넣었다.
// 처음 만들 때는 참조 페이지가 없어 일부러 카드를 안 뒀다 — 내용 없는
// 페이지로 링크를 걸면 방문자 신뢰도 떨어지고 검색엔진도 빈약한 페이지로
// 취급하기 때문이다. 지금은 여섯 곳 다 실제 내용이 있어 입구를 연다.
// ⚠️ 카드는 **내용이 있는 페이지만** 건다. 새 페이지를 만들 때 카드부터
//    추가하지 말 것.
import IdLayout from '../../layouts/IdLayout.astro';
import id from '../../i18n/id.json';
import { withBase } from '../../utils/site';
import newsData from '../../data/id/news.json';

// ── AEO/GEO — 홈에서 바로 답이 되는 블록 (2026-09-07) ──────────
// 답변 엔진은 "이 사이트는 무엇이고 누구를 위한 것인가"를 물었을 때
// **그 답이 실제로 적혀 있는 페이지**를 인용한다. 홈이 소개 문단만
// 있으면 인용할 문장이 없다. 그래서 사이트 자체에 대한 문답을
// 화면에 그대로 싣고 FAQPage로 표시한다.
//
// ⚠️ 여기 답변은 /id/faq/ 의 제도 문답과 **내용이 겹치지 않는다.**
//    저쪽은 "AI를 도입할 때 무엇을 확인하나", 여기는 "이 사이트는
//    무엇인가"다. 같은 문답을 두 곳에 복사하면 안 된다.
// ⚠️ 답변에 적은 사실(수집처·갱신 주기)은 scripts/ 의 수집기와
//    .github/workflows/ 의 cron 에서 확인한 것이다. 수집처나 주기를
//    바꾸면 이 문구도 함께 고칠 것.
const recentNews = newsData.items.slice(0, 5);

const siteRoot = Astro.site ? Astro.site.href.replace(/\/$/, '') : 'https://hospital-ai-lab.com';
const pageUrl = `${siteRoot}${withBase('/id/')}`;

const homeFaqLd = {
  '@type': 'FAQPage',
  '@id': `${pageUrl}#home-faq`,
  url: pageUrl,
  inLanguage: 'id',
  isPartOf: { '@id': `${siteRoot}/#website-id` },
  mainEntity: id.home.homeFaq.map((f, i) => ({
    '@type': 'Question',
    '@id': `${pageUrl}#home-q${i + 1}`,
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};
---

<IdLayout
  title={id.home.metaTitle}
  description={id.home.metaDescription}
  koPath="/"
  twPath="/tw/"
  jaPath="/ja/"
  ruPath="/ru/"
  schema={[homeFaqLd]}
>
  <p class="id-updated" style="letter-spacing:.06em">{id.home.heroEyebrow}</p>
  <h1>{id.home.heroTitle}</h1>
  <p>{id.home.heroLead}</p>

  <p style="margin-top:16px">
    <a class="id-cta" href={withBase('/id/about/')}>{id.nav.about} →</a>
  </p>

  <div class="id-cards">
    <a class="id-card" href={withBase('/id/ai-digest/')}>
      <span class="id-card-t">{id.nav.aiDigest}</span>
      <span class="id-card-d">{id.home.aiDigestDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/glossary/')}>
      <span class="id-card-t">{id.nav.glossary}</span>
      <span class="id-card-d">{id.home.glossaryDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/faq/')}>
      <span class="id-card-t">{id.nav.faq}</span>
      <span class="id-card-d">{id.home.faqDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/news/')}>
      <span class="id-card-t">{id.nav.news}</span>
      <span class="id-card-d">{id.home.newsDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/events/')}>
      <span class="id-card-t">{id.nav.events}</span>
      <span class="id-card-d">{id.home.eventsDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/gov-support/')}>
      <span class="id-card-t">{id.nav.govSupport}</span>
      <span class="id-card-d">{id.home.govSupportDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/videos/')}>
      <span class="id-card-t">{id.nav.videos}</span>
      <span class="id-card-d">{id.home.videosDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/blog/')}>
      <span class="id-card-t">{id.nav.blog}</span>
      <span class="id-card-d">{id.home.blogDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/checklist/')}>
      <span class="id-card-t">{id.nav.checklist}</span>
      <span class="id-card-d">{id.home.checklistDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/guide/')}>
      <span class="id-card-t">{id.nav.guide}</span>
      <span class="id-card-d">{id.home.guideDesc}</span>
    </a>
    <a class="id-card" href={withBase('/id/tips/')}>
      <span class="id-card-t">{id.nav.tips}</span>
      <span class="id-card-d">{id.home.tipsDesc}</span>
    </a>
  </div>

  {/* 최신 뉴스 — 홈에 실제 내용이 있어야 검색·답변 엔진이 인용할 거리가 생긴다.
      한국어판 홈이 이미 쓰는 방식이고, 다른 판에는 빠져 있었다(2026-09-07 추가). */}
  {
    recentNews.length > 0 && (
      <section class="id-home-sec">
        <div class="id-sec-head">
          <h2>{id.home.homeNewsTitle}</h2>
          <a class="id-more" href={withBase('/id/news/')}>{id.home.homeNewsMore}</a>
        </div>
        <ul class="id-news-preview">
          {recentNews.map((item) => (
            <li>
              <a href={item.link} target="_blank" rel="noopener">
                {item.title}
                <span class="sr-only"> ({id.ui.external})</span>
              </a>
              {item.source && <span class="id-src">{item.source}</span>}
            </li>
          ))}
        </ul>
      </section>
    )
  }

  {/* 사이트 자체에 대한 문답. /id/faq/ 의 제도 문답과 내용이 겹치지 않는다. */}
  <section class="id-home-sec">
    <div class="id-sec-head">
      <h2>{id.home.homeFaqTitle}</h2>
      <a class="id-more" href={withBase('/id/faq/')}>{id.home.homeFaqMore}</a>
    </div>
    <dl class="id-home-faq">
      {
        id.home.homeFaq.map((f) => (
          <>
            <dt>{f.q}</dt>
            <dd>{f.a}</dd>
          </>
        ))
      }
    </dl>
  </section>

  <h2>{id.home.notTranslationTitle}</h2>
  <p>{id.home.notTranslationBody}</p>

  <h2>{id.home.sourceNoteTitle}</h2>
  <p>{id.home.sourceNoteBody}</p>

  <div class="id-note">
    <p><strong>{id.home.koNoticeTitle}</strong></p>
    <p style="margin-top:6px">{id.home.koNoticeBody}</p>
    <p style="margin-top:8px"><a href={withBase('/')} hreflang="ko">{id.ui.langKo} →</a></p>
  </div>
</IdLayout>

<style>
  /* .id-cta 는 마크업에만 있고 스타일이 없어서 평범한 링크로 나왔다
     (2026-09-06 색상 교체 중 발견). ja·ru 홈과 같은 버튼 모양으로 맞춘다. */
  .id-cta {
    display: inline-block;
    background: var(--id-btn-bg);
    color: var(--id-btn-fg);
    text-decoration: none;
    padding: 10px 22px;
    border-radius: 4px;
    font-size: 14.5px;
  }
  .id-cta:hover { background: var(--id-btn-bg-hover); }

  .id-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
    gap: 12px;
    margin: 26px 0 8px;
  }
  .id-card {
    display: flex;
    flex-direction: column;
    gap: 6px;
    border: 1px solid var(--id-border);
    background: var(--id-base);
    padding: 14px 16px;
    text-decoration: none;
    color: var(--id-text);
  }
  .id-card:hover {
    background: var(--id-surface);
    border-color: var(--id-primary);
  }
  .id-card-t {
    font-weight: 700;
    color: var(--id-primary);
    font-size: 15px;
  }
  .id-card-d {
    font-size: 13px;
    line-height: 1.8;
    color: var(--id-text-sub);
  }

  .id-wip {
    margin-top: 28px;
    padding-top: 16px;
    border-top: 1px solid var(--id-border);
    font-size: 13.5px;
    color: var(--id-text-sub);
  }

  /* 홈 뉴스·문답 (2026-09-07 AEO/GEO) */
  .id-home-sec {
    margin-top: 30px;
    padding-top: 18px;
    border-top: 1px solid var(--id-border);
  }
  .id-sec-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 12px;
    flex-wrap: wrap;
  }
  .id-sec-head h2 {
    margin: 0;
  }
  .id-more {
    font-size: 13px;
    color: var(--id-primary);
    text-decoration: none;
    white-space: nowrap;
  }
  .id-more:hover { text-decoration: underline; }
  .id-news-preview {
    list-style: none;
    margin: 14px 0 0;
    padding: 0;
  }
  .id-news-preview li {
    padding: 9px 0;
    border-bottom: 1px solid var(--id-border);
    font-size: 14px;
    line-height: 1.7;
  }
  .id-news-preview a {
    color: var(--id-text);
    text-decoration: none;
  }
  .id-news-preview a:hover { color: var(--id-primary); text-decoration: underline; }
  .id-src {
    display: inline-block;
    margin-left: 8px;
    font-size: 12px;
    color: var(--id-text-sub);
  }
  .id-home-faq {
    margin: 14px 0 0;
  }
  .id-home-faq dt {
    font-weight: 700;
    color: var(--id-primary);
    margin-top: 16px;
    font-size: 14.5px;
    line-height: 1.7;
  }
  .id-home-faq dt:first-child { margin-top: 0; }
  .id-home-faq dd {
    margin: 6px 0 0;
    font-size: 14px;
    line-height: 1.9;
    color: var(--id-text);
  }
</style>

---
// Video Rekomendasi — 러시아어판 ru/videos.astro와 같은 구조 (2026-09-06 신설).
//
// 영상 자체는 한국어판·일본어판·러시아어판과 같은 12편(연구소장이 직접 만든
// 입문 시리즈)이고, 제목만 인도네시아어로 옮겼다. 음성이 한국어라는 사실은
// 재생 전에 알린다 — 보고 나서 알게 하면 신뢰를 해친다.
import IdLayout from '../../layouts/IdLayout.astro';
import { videosId, videosMetaId } from '../../data/id/pages';
import idI18n from '../../i18n/id.json';
import { YOUTUBE_CHANNEL_URL } from '../../utils/site';

const p = videosMetaId;

// ⚠️ VideoObject는 쓰지 않는다. uploadDate·duration을 정확히 모르기 때문이다
//    (recommended-videos.json의 addedAt은 사이트 추가일이지 유튜브 공개일이
//    아니다). 화면에 보이는 목록 순서를 ItemList로만 전달한다 — ru판과 동일.
const schema = [
  {
    '@type': 'ItemList',
    name: p.pageTitle,
    numberOfItems: videosId.length,
    itemListElement: videosId.map((v, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: v.titleId,
      url: `https://www.youtube.com/watch?v=${v.videoId}`,
    })),
  },
];
---

<IdLayout
  title={p.title}
  description={p.description}
  crumb={idI18n.nav.videos}
  koPath="/youtube/"
  jaPath="/ja/videos/"
  ruPath="/ru/videos/"
  schema={schema}
>
  <h1>{p.pageTitle}</h1>
  <p class="id-lead">{p.lead}</p>

  {/* 언어 안내는 재생 전에 */}
  <p class="id-notice">{p.notice}</p>

  <p class="id-count">Jumlah video: {videosId.length}</p>

  <ul class="id-videos">
    {
      videosId.map((v) => (
        <li>
          <a href={`https://www.youtube.com/watch?v=${v.videoId}`} target="_blank" rel="noopener">
            <img
              src={`https://img.youtube.com/vi/${v.videoId}/hqdefault.jpg`}
              alt=""
              loading="lazy"
              width="320"
              height="180"
            />
            <div class="id-video-text">
              <span class="id-video-title">{v.titleId}</span>
              {v.note && <span class="id-video-note">{v.note}</span>}
              <span class="id-video-ext">
                Tonton di YouTube<span class="sr-only"> ({idI18n.ui.external})</span>
              </span>
            </div>
          </a>
        </li>
      ))
    }
  </ul>

  <p class="id-channel">
    <a href={YOUTUBE_CHANNEL_URL} target="_blank" rel="noopener">
      {idI18n.profile.youtube}<span class="sr-only"> ({idI18n.ui.external})</span>
    </a>
  </p>
</IdLayout>

<style>
  .id-lead {
    color: var(--id-text-sub);
  }
  .id-notice {
    border: 1px solid var(--id-border);
    border-left: 4px solid var(--id-primary);
    background: var(--id-surface);
    padding: 12px 14px;
    font-size: 13.5px;
    line-height: 1.85;
    margin-bottom: 18px;
  }
  .id-count {
    font-size: 13px;
    color: var(--id-text-sub);
    margin-bottom: 16px;
  }
  .id-videos {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 14px;
  }
  .id-videos a {
    display: flex;
    gap: 14px;
    align-items: flex-start;
    border: 1px solid var(--id-border);
    background: var(--id-base);
    padding: 12px;
    text-decoration: none;
    color: var(--id-text);
  }
  .id-videos a:hover {
    background: var(--id-surface);
    border-color: var(--id-primary);
  }
  .id-videos img {
    width: 160px;
    height: 90px;
    object-fit: cover;
    flex-shrink: 0;
    background: var(--id-surface);
  }
  .id-video-text {
    display: flex;
    flex-direction: column;
    gap: 5px;
    min-width: 0;
  }
  .id-video-title {
    font-size: 14.5px;
    line-height: 1.7;
    color: var(--id-primary);
  }
  .id-video-note {
    font-size: 13px;
    color: var(--id-text-sub);
    line-height: 1.8;
  }
  .id-video-ext {
    font-size: 12px;
    color: var(--id-text-sub);
  }
  .id-channel {
    margin-top: 24px;
    padding-top: 16px;
    border-top: 1px solid var(--id-border);
    font-size: 14px;
  }

  @media (max-width: 560px) {
    .id-videos a {
      flex-direction: column;
    }
    .id-videos img {
      width: 100%;
      height: auto;
      aspect-ratio: 16 / 9;
    }
  }
</style>

---
// 대만어판(정체자) 본문 섹션 공통 렌더러.
// guide·checklist·tips가 같은 {heading, body, items} 구조를 쓴다.
// ru판 TwSections.astro와 동일한 패턴.
import type { TwSection } from '../data/tw/pages';

interface Props {
  sections: TwSection[];
  /** 항목 앞에 체크박스 모양을 붙일지(체크리스트용) */
  checkbox?: boolean;
}

const { sections, checkbox = false } = Astro.props;

// 한자 제목은 URL 앵커로 쓰기 어려우므로 순번 기반 id를 쓴다.
const anchor = (i: number) => `s${i + 1}`;
---

<div class="tw-sections">
  {
    sections.map((s, i) => (
      <section class="tw-section" id={anchor(i)}>
        <h2>
          <a href={`#${anchor(i)}`} class="tw-anchor" aria-label={`連到「${s.heading}」這一節`}>
            {s.heading}
          </a>
        </h2>
        {s.body && <p class="tw-section-body">{s.body}</p>}
        {s.items.length > 0 && (
          <ul class={checkbox ? 'tw-check' : 'tw-list'}>
            {s.items.map((it) => (
              <li>{it}</li>
            ))}
          </ul>
        )}
      </section>
    ))
  }
</div>

<style>
  .tw-sections {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
  .tw-section {
    scroll-margin-top: 20px;
  }
  .tw-section h2 {
    font-size: 17px;
    color: var(--tw-primary);
    border-left: 4px solid var(--tw-primary);
    padding-left: 10px;
    margin-bottom: 12px;
  }
  .tw-anchor {
    color: inherit;
    text-decoration: none;
  }
  .tw-anchor:hover {
    text-decoration: underline;
    text-underline-offset: 4px;
  }
  .tw-section-body {
    margin-bottom: 12px;
  }
  .tw-list,
  .tw-check {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .tw-list li,
  .tw-check li {
    position: relative;
    padding-left: 22px;
    font-size: 14.5px;
    line-height: 1.9;
  }
  .tw-list li::before {
    content: '–';
    position: absolute;
    left: 4px;
    color: var(--tw-primary);
  }
  .tw-check li::before {
    content: '';
    position: absolute;
    left: 2px;
    top: 0.55em;
    width: 11px;
    height: 11px;
    border: 1.5px solid var(--tw-primary);
  }
  .tw-check li {
    border-bottom: 1px dotted var(--tw-border);
    padding-bottom: 8px;
  }
  .tw-check li:last-child {
    border-bottom: none;
  }
</style>

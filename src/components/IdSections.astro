---
// 인도네시아어판 본문 섹션 공통 렌더러.
// guide·checklist·tips가 같은 {heading, body, items} 구조를 쓴다.
// ru판 IdSections.astro와 동일한 패턴.
import type { IdSection } from '../data/id/pages';

interface Props {
  sections: IdSection[];
  /** 항목 앞에 체크박스 모양을 붙일지(체크리스트용) */
  checkbox?: boolean;
}

const { sections, checkbox = false } = Astro.props;

// 인도네시아어 제목은 URL 앵커로 쓰기 어려우므로 순번 기반 id를 쓴다.
const anchor = (i: number) => `s${i + 1}`;
---

<div class="id-sections">
  {
    sections.map((s, i) => (
      <section class="id-section" id={anchor(i)}>
        <h2>
          <a href={`#${anchor(i)}`} class="id-anchor" aria-label={`Tautan ke bagian ${s.heading}`}>
            {s.heading}
          </a>
        </h2>
        {s.body && <p class="id-section-body">{s.body}</p>}
        {s.items.length > 0 && (
          <ul class={checkbox ? 'id-check' : 'id-list'}>
            {s.items.map((it) => (
              <li>{it}</li>
            ))}
          </ul>
        )}
      </section>
    ))
  }
</div>

<style>
  .id-sections {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }
  .id-section {
    scroll-margin-top: 20px;
  }
  .id-section h2 {
    font-size: 17px;
    color: var(--id-primary);
    border-left: 4px solid var(--id-primary);
    padding-left: 10px;
    margin-bottom: 12px;
  }
  .id-anchor {
    color: inherit;
    text-decoration: none;
  }
  .id-anchor:hover {
    text-decoration: underline;
    text-underline-offset: 4px;
  }
  .id-section-body {
    margin-bottom: 12px;
  }
  .id-list,
  .id-check {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .id-list li,
  .id-check li {
    position: relative;
    padding-left: 22px;
    font-size: 14.5px;
    line-height: 1.9;
  }
  .id-list li::before {
    content: '–';
    position: absolute;
    left: 4px;
    color: var(--id-primary);
  }
  .id-check li::before {
    content: '';
    position: absolute;
    left: 2px;
    top: 0.55em;
    width: 11px;
    height: 11px;
    border: 1.5px solid var(--id-primary);
  }
  .id-check li {
    border-bottom: 1px dotted var(--id-border);
    padding-bottom: 8px;
  }
  .id-check li:last-child {
    border-bottom: none;
  }
</style>

-- 마이그레이션: "AI로 만든 앱" 업로드 용량 표시를 실제 상한(50MB)에 맞게 수정
-- setup.sql을 이미 한 번 실행한 적이 있다면, 전체를 다시 실행하지 말고
-- 이 파일만 SQL Editor에 붙여넣어 실행하세요.
--
-- 배경: migrate-2026-07-10-ai-apps-files.sql에서 버킷 용량을 200MB로
-- 설정했지만, Supabase 무료 요금제는 프로젝트 전체에 50MB 절대 상한이
-- 걸려 있어(실측 확인: 50MB 성공, 51MB 거부) 실제로는 항상 50MB에서
-- 막히고 있었다. 버킷 설정을 실제 동작에 맞게 50MB로 고쳐 오해를 없앤다
-- (동작 자체는 이미 50MB로 막혀 있었으므로 이 마이그레이션 전후로 실제
-- 업로드 가능 여부는 달라지지 않는다).
update storage.buckets
set file_size_limit = 52428800
where id = 'ai-apps';
